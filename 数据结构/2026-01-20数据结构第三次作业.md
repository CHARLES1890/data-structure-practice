一、
（1）C
（2）C
（3）A
（4）B
（5）A
二、
```python
def deleteFox(s):
	top = 0
	res = []
	for i in s:
		if len(res) > 1 and res[-2] == 'f' and res[-1] == 'o' and i == 'x':
			res.pop()
			res.pop()
		else:
			res.append(i)
	print(''.join(res))			
```
测试：
```python
deleteFox('firefox')
deleteFox('foffoxoxxa')
deleteFox('ffofoxfoxxffoxoxind')
```
输出结果：
```
fire
a
find
```